import styled from "@emotion/styled";
import { Popover } from "@mui/material";

const Wrapper = styled(Popover)`
  .MuiPaper-root {
    box-shadow: -26px 24px 26px 1px rgba(88, 84, 132, 0.08);
    border-radius: 5px !important;
  }
`;

export default Wrapper;
